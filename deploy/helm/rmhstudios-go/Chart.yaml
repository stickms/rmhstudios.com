apiVersion: v2
name: rmhstudios-go
description: >-
  rmhstudios.com Go microservices fleet — the Stage-4 (per-PR-#121 roadmap) Go
  rewrite of the realtime/worker tier, deployed alongside the React SSR web app.
  Deliberately mirrors the conventions of the rmhstudios chart from PR #121
  (generic Deployment over a services map, Traefik ingress, reused Secret).
type: application
version: 0.1.0
appVersion: "1.0.0"
