/**
 * Shared Tailwind micro-interaction constants for the feed/discovery cards.
 * Kept here so the exact class string has a single source of truth and stays
 * consistent across the hand-tuned discovery grids.
 */

/** Hairline card that lifts subtly on hover. */
export const LIFT_CARD =
  'transition-[transform,border-color] duration-200 hover:-translate-y-0.5 hover:border-site-accent/50';
