/**
 * RMHbrowser — Main Page
 *
 * Full-featured web browser with tabs, bookmarks, history, themes, and profiles.
 */
'use client';

import BrowserUI from '@/components/rmhbrowser/BrowserUI';

export default function RmhBrowserPage() {
  return <BrowserUI />;
}
