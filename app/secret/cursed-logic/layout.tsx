export const metadata = {
  title: 'Cursed Logic | RMH Studios',
  description: 'A turn-based duel against an unstable Protocol.',
};

export default function CursedLogicLayout({
  children,
}: { children: React.ReactNode }) {
  return children;
}
