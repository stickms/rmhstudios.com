import { WeatherDashboard } from '@/components/rmh-weather/WeatherDashboard';

export const metadata = {
  title: 'RMHWeather | Premium Weather Experience',
  description: 'Real-time weather data with a stunning, immersive glassmorphic interface.',
};

export default function RMHWeatherPage() {
  return <WeatherDashboard />;
}
