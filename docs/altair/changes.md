1. For the floor tileset, use all the tiles except for the ones at "coords" [0,3] [2,2] and [2,3] as they look like obstacles
2. For "slicing" or "slashing" weapons (e.g. sword, whip, etc.) instead of a "cone" use an actual slash animation (maybe find sprites for this?)
3. For weapons that drop "pools" also use more obvious sprites
4. It is really hard to die as the player, make basic enemies deal more damage
5. Allow players to select "double-time" mode from the class select page for solo play
6. For locked classes, when a player hovers over their card tell them how to unlock that class
7. In the multiplayer lobby, allow players to select their class and display their selected class on the player list
8. From the multiplayer lobby, the "copy" button should copy the full invite link
9. Allow lobby hosts to change lobby settings from within the lobby page itself, as well as kicking/banning and transferring host to other users
10. Lobby chat should use the same shared chat component from rmhstudy/rmhtube/rmhtype with reactions, replies, and gif support (and add those to the rmhstudy/rmhtube/rmhtype if theyre not already there) 
11. move the weapons/items hud to below the main health/xp header instead of being at the bottom of the screen
12. Add more pickups (I see the sprites are already in the sprites folder) that have different effects for the player/game
13. Show minimap on tab
